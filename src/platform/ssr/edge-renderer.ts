export interface EdgeRenderOptions {
  url: string;
}

export async function renderToStringTree(_options: EdgeRenderOptions): Promise<string> {
  // Placeholder: real implementation will render app to HTML on the edge.
  return '<!-- ssr not implemented -->';
}

